# direnv configuration for search-engine module
# Automatically activates when entering the directory

# Load Poetry environment
layout poetry

# Set Python path for development
export PYTHONPATH="${PWD}/src:${PYTHONPATH:-}"

# Google Cloud settings for development
# export GOOGLE_CLOUD_PROJECT="your-project-id"
# export GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"

# Development environment
export ENVIRONMENT="development"
export LOG_LEVEL="INFO"

echo "🔍 search-engine module environment activated"
echo "   Python path: ${PYTHONPATH}"
echo "   Use 'make help' to see available commands"
