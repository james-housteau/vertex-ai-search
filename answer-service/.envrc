# Development environment for answer-service
# This file is automatically loaded by direnv when entering the directory

# Python environment
export PYTHONPATH="$PWD/src:$PYTHONPATH"

# Google Cloud configuration
export GOOGLE_CLOUD_PROJECT="${GOOGLE_CLOUD_PROJECT:-vertex-ai-search-dev}"
export GOOGLE_APPLICATION_CREDENTIALS="${GOOGLE_APPLICATION_CREDENTIALS:-}"

# Development settings
export PYTHONDONTWRITEBYTECODE=1
export PYTHONUNBUFFERED=1

# Poetry configuration
export POETRY_VENV_IN_PROJECT=1

echo "🔧 answer-service development environment loaded"
echo "📦 Python path: $PYTHONPATH"
echo "☁️  GCP Project: $GOOGLE_CLOUD_PROJECT"
